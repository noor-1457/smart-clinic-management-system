@{
    Layout = null;
}

<div class="bg-white rounded-lg shadow-md p-6">
    <h2 class="text-2xl font-bold text-red-600 mb-4">📋 Reports</h2>
    
    <div class="space-y-4">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <button onclick="generateReport('appointments')" 
                    class="p-4 border-2 border-red-200 rounded-lg hover:bg-red-50 transition text-left">
                <div class="font-semibold text-red-600">📅 Appointment Report</div>
                <div class="text-sm text-gray-600 mt-1">Generate appointment summary</div>
            </button>
            
            <button onclick="generateReport('patients')" 
                    class="p-4 border-2 border-blue-200 rounded-lg hover:bg-blue-50 transition text-left">
                <div class="font-semibold text-blue-600">👥 Patient Report</div>
                <div class="text-sm text-gray-600 mt-1">Patient statistics and records</div>
            </button>
            
            <button onclick="generateReport('revenue')" 
                    class="p-4 border-2 border-green-200 rounded-lg hover:bg-green-50 transition text-left">
                <div class="font-semibold text-green-600">💰 Revenue Report</div>
                <div class="text-sm text-gray-600 mt-1">Financial summary and analysis</div>
            </button>
            
            <button onclick="generateReport('inventory')" 
                    class="p-4 border-2 border-purple-200 rounded-lg hover:bg-purple-50 transition text-left">
                <div class="font-semibold text-purple-600">💊 Inventory Report</div>
                <div class="text-sm text-gray-600 mt-1">Medicine stock levels</div>
            </button>
        </div>
    </div>
</div>

<script>
function generateReport(type) {
    alert(`Generating ${type} report...`);
    // Add your report generation logic here
}
</script>

